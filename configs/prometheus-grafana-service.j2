apiVersion: v1
kind: Service
metadata:
  name: prometheus-grafana-{{ TAG_PLACEHOLDER }}
  labels:
    app: prometheus-grafana
    revision: {{ TAG_PLACEHOLDER }}
spec:
  ports:
    - name: http
      port: 3000
      targetPort: 3000
  selector:
    app: prometheus-grafana
    revision: {{ TAG_PLACEHOLDER }}
  type: NodePort
